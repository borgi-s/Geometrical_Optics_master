# Variant: sample remounted in orientation S2.
# Equivalent to default.toml except for the [crystal.wall].sample_remount field.
# Used by the Purdue 2024 paper to compare the same defect across symmetry-
# equivalent mountings.

[reciprocal]
hkl = [-1, 1, -1]   # Al 111 reflection — preserves pre-D implicit default
keV = 17.0

[scan.phi]
range = 6e-4
steps = 61

[scan.chi]
range = 2e-3
steps = 61

[crystal]
mode = "wall"

[crystal.wall]
dis = 4.0          # inter-dislocation distance (µm)
ndis = 151         # number of dislocations
sample_remount = "S2"  # one of "S1", "S2", "S3", "S4" — Purdue 2024 paper.

[io]
fn_prefix = "/mosa_test_0000_"
ftype = ".npy"
dislocs_dirname = "images10"
perfect_dirname = "images10_perf_crystal"
include_perfect_crystal = true
# max_workers = 4  # uncomment to cap parallel forward() calls; None = auto

[postprocess]
enabled = true
chi_oversample = 20
phi_oversample = 20
chi_oversample_for_shift = 100
figures_dirname = "figures"
data_dirname = "analysis"
